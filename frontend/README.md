# Frontend for auth-app

Steps to run this project:

1. Run `npm i` command
3. Run `npm start` or `npm dev` command

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
